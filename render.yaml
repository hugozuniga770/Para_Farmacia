services:
  - type: web
    name: proyecto-farmacia
    # NO hay 'env: docker'. Render lo detectará como Java.
    plan: free
    branch: maestro
    buildCommand: "mvn clean package"
    startCommand: "java -jar target/dependency/webapp-runner.jar --port $PORT target/*.war"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: farmacia-db
          property: connectionString

databases:
  - name: farmacia-db
    plan: free